# Cleaned Data

Run the pipeline after adding raw CSV files:

```bash
python -m src.run_pipeline
```

This folder will contain:

- cleaned table-level CSVs
- `olist_analytical_dataset.csv`
- `rfm_segments.csv`
- `monthly_cohort_retention.csv`
- `executive_kpis.csv`
